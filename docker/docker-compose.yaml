# ==================================================================
# WARNING: This file is auto-generated by generate_docker_compose
# Do not modify this file directly. Instead, update the .env_dev.example
# or docker-compose-template_dev.yaml and regenerate this file.
# ==================================================================

x-shared-env: &shared-api-worker-env
  COMPOSE_PROJECT_NAME: ${COMPOSE_PROJECT_NAME:-servo_ai}
  DB_HOST: ${DB_HOST:-10.51.3.198}
  DB_PORT: ${DB_PORT:-35744}
  DB_NAME: ${DB_NAME:-iot_platform}
  DB_USER: ${DB_USER:-root}
  DB_PASSWORD: ${DB_PASSWORD:-nbt^O35*d=X!}
  REDIS_HOST: ${REDIS_HOST:-10.51.2.32}
  REDIS_PORT: ${REDIS_PORT:-6379}
  REDIS_PASSWORD: ${REDIS_PASSWORD:-23ob4WeHlhkR}
  REDIS_USE_SSL: ${REDIS_USE_SSL:-false}
  REDIS_DB: ${REDIS_DB:-0}
  CELERY_BROKER_URL: ${CELERY_BROKER_URL:-redis://:23ob4WeHlhkR@10.51.2.32:6379/0}
  CELERY_RESULT_BACKEND: ${CELERY_RESULT_BACKEND:-redis://:23ob4WeHlhkR@10.51.2.32:6379/0}
  CELERY_TIMEZONE: ${CELERY_TIMEZONE:-Asia/Shanghai}
  CELERY_TASK_SERIALIZER: ${CELERY_TASK_SERIALIZER:-json}
  CELERY_RESULT_SERIALIZER: ${CELERY_RESULT_SERIALIZER:-json}
  CELERY_ACCEPT_CONTENT: ${CELERY_ACCEPT_CONTENT:-["json"]}
  CELERY_RESULT_EXPIRES: ${CELERY_RESULT_EXPIRES:-3600}
  CELERY_WORKER_AMOUNT: ${CELERY_WORKER_AMOUNT:-1}
  CELERY_AUTO_SCALE: ${CELERY_AUTO_SCALE:-false}
  CELERY_MAX_WORKERS: ${CELERY_MAX_WORKERS:-}
  CELERY_MIN_WORKERS: ${CELERY_MIN_WORKERS:-}
  CELERY_USE_SENTINEL: ${CELERY_USE_SENTINEL:-false}
  CELERY_SENTINEL_MASTER_NAME: ${CELERY_SENTINEL_MASTER_NAME:-}
  CELERY_SENTINEL_SOCKET_TIMEOUT: ${CELERY_SENTINEL_SOCKET_TIMEOUT:-0.1}
  LOG_LEVEL: ${LOG_LEVEL:-DEBUG}
  LOG_FILE_MAX_SIZE: ${LOG_FILE_MAX_SIZE:-20}
  LOG_FILE_BACKUP_COUNT: ${LOG_FILE_BACKUP_COUNT:-5}
  LOG_DIR: ${LOG_DIR:-/app/api/logs}
  LOG_TZ: ${LOG_TZ:-Asia/Shanghai}
  LOG_FORMAT: ${LOG_FORMAT:-%(asctime)s,%(msecs)d %(levelname)-2s [%(filename)s:%(lineno)d] %(message)s}
  DIFY_BASE_URL: ${DIFY_BASE_URL:-http://xuntian-ai-sit.tclpv.com}
  DIFY_API_KEY: ${DIFY_API_KEY:-app-FpC7jmVhoS90BTUSfCxsm0gG}
  DIFY_TIMEOUT: ${DIFY_TIMEOUT:-300}
  DIDY_OCR_BASE_URL: ${DIDY_OCR_BASE_URL:-https://xuntian-hw-dev.tclpv.com/}

services:
  api:
    image: server_ai-api:dev
    build: 
      context: ../api
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      # Use the shared environment variables.
      <<: *shared-api-worker-env
      # Startup mode, 'api' starts the API server.
      MODE: api
    env_file:
      - ./.env
    volumes:
      # Mount the storage directory to the container, for storing user files.
      - ./volumes/app/storage:/app/api/storage
      - ./volumes/api/logs:/app/api/logs
    networks:
      - servo_net

  worker:
    image: server_ai-worker:dev
    build: 
      context: ../api
      dockerfile: Dockerfile
    environment:
      <<: *shared-api-worker-env
      CELERY_AUTO_SCALE: "true"
      CELERY_MAX_WORKERS: 4
      CELERY_MIN_WORKERS: 2
      # 增加延迟确认配置
      CELERY_ACKS_LATE: True  
      # 添加预取限制
      CELERYD_PREFETCH_MULTIPLIER: 1  
      CELERY_LOG_FILE: /app/worker/logs/celery.log
      CELERY_LOG_DIR: /app/worker/logs
      MODE: worker
    command: sh -c "mkdir -p $$CELERY_LOG_DIR && celery -A src.celery_app:app worker --autoscale=$$CELERY_MAX_WORKERS,$$CELERY_MIN_WORKERS --loglevel=info --logfile=$$CELERY_LOG_FILE"
    env_file:
      - ./.env
    volumes:
      - ./volumes/worker/logs:/app/worker/logs
    networks:
      - servo_net
  beat:
      image: server_ai-beat:dev
      build: 
        context: ../api
        dockerfile: Dockerfile
      environment:
        <<: *shared-api-worker-env
        CELERY_LOG_FILE: /app/beat/logs/celerybeat.log
        CELERY_LOG_DIR: /app/beat/logs
        MODE: beat
      command: sh -c "mkdir -p $$CELERY_LOG_DIR && celery -A src.celery_app:app beat --loglevel=info --logfile=$$CELERY_LOG_FILE"
      env_file:
        - ./.env
      volumes:
        - ./volumes/beat/logs:/app/beat/logs
      networks:
      - servo_net
networks:
  servo_net:
    driver: bridge



