prompt = f"""
        任务：计算文本相似度并返回小数列表
        1. 比较内容："{content}"
        2. 比较对象列表：{list(order_advices)}
        3. 输出要求：
           - 仅返回JSON格式的小数列表，不包含任何解释文字
           - 示例输出：[0.85, 0.6, 0.32]
        """
        
prompt = f"""
        任务：计算文本相似度并返回百分比列表
        1. 比较内容："{content}"
        2. 比较对象列表：{list(order_advices)}
        3. 将content和orderAdvices 中的文本分别进行文本相似度计算
        4. 输出要求：
           - 仅返回JSON格式的百分比列表，不包含任何解释文字
           - 示例输出：["85%", "40%", "30%"]
           - 确保列表元素用双引号包裹，逗号分隔
        """
        
    # @cached(ttl=3600)  # 异步缓存替代lru_cache